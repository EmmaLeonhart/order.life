# Commentary from narrative_identity — running log, newest at the bottom

## 2026-09-25 — `summaries/` is all stale

A read-only check of all 32 files in `summaries/`: they are plain file bundles (a hand-run PowerShell
concatenation, February 2026, made for pasting into an LLM), not summaries. 31 match tracked files
exactly at the commit that added them (af89257f8); the 32nd, `Comprehensive_Site_Guide.md`, is a
render of `content/i18n/en.json` and `content/gaian_days.json`. **Nothing needs extracting; all 32 can
go** — about 5.6 MB of outdated duplicates that pollute every search. *Our judgement.* Emma: stale
material "really does need to go." **Not deleted by narrative_identity** — we only write here.
