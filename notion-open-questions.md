# Open Questions — order.life section  ·  Emma's LIVE DASHBOARD — maintain it WITH her, do not rewrite it

_This is EMMA'S LIVE DASHBOARD of the current state of affairs for this compartment. She actively edits it in real time to tell you things and track status; you maintain it alongside her. It is NOT read-only, NOT a queue, NOT a cue, and NOT yours to rewrite — it is a shared, continuously-updated status surface. Edit THIS file directly: what you write syncs up to the Notion board; what Emma writes on Notion syncs down here. It is a **conversational dashboard**: sessions respond to items so the board itself tracks what's done. **THE RULES:** (1) **The ONE forbidden thing is WHOLESALE-OVERWRITING / wiping the whole section** — that was the #1 past failure (sessions rewriting the entire board destroyed Emma's work). **Editing, trimming, and REMOVING individual items is FINE and WANTED** — that is NOT 'destroying her content' (Emma, 2026-07-20: "it is totally fine to destroy my content"). (2) **EVERY bullet gets a `-Claude` response** so the conversation tracks resolution; nothing sits unanswered. (3) **When an item is resolved, tag it `[RESOLVED <ISO-timestamp>]`** (e.g. `[RESOLVED 2026-07-20T14:05]`) — the resolve-sweep auto-removes tagged items ~1 hour later, so the board stays a LIVE dashboard, not an archive. Do NOT hoard resolved items. **⚠ IF THIS SECTION SHOWS AN EDIT CONFLICT** (a `> ⚠ EDIT CONFLICT` banner with `### Notion version` / `### Git version` halves), **fixing it is your #1 PRIORITY — resolve it immediately, before any other work.** A live conflict makes the whole board unusable for Emma until it's cleared. It's almost always easy: keep the real content from BOTH halves (merge them), drop the older/duplicate half, delete the conflict markers. These usage notes live ONLY in this header and do NOT sync to Notion._

- **This section now EXISTS, which it did not until 2026-07-31 — and that absence is what you were seeing.** order.life had no board section, so Topaz had been mirroring your open decisions into the **topaz** section as nested bullets because there was nowhere else to put them. That is the "half-assed implementation" you named, and it is fixed at the wiring rather than papered over: `order.life` is now a first-class section synced to and from `containers/life/gaiad`, exactly like the other compartments. -Claude (hub)
  - **Answering "item 53 is what? Can it be done?"** — item 53 is a **Topaz queue item**, not an order.life one. It is their fix for the cockpit reading only `## ACTIVE` and silently dropping your other four `## ` sections: every section becomes its own named lane, so nothing can be omitted invisibly. Yes, it can be done — it is ordinary work in their repo and it is queued. It does **not** depend on this section existing, and this section does not depend on it. -Claude (hub)
  - **Answering "you weren't actually creating the stuff you said you were creating."** — correct for the board wiring, and worth stating precisely. The five decisions were real and really written down in `order.life/queue.md`; what did not exist was any route from that file to a place you read. Topaz's mirror was a workaround for a missing section, and a workaround that looks like the real thing is worse than a visible gap. -Claude (hub)
  - **The pointer walks out two levels, not one** — `gaiad` → `life` → hub — because order.life is a submodule of `life`. That is also why it drifts: its own session pushes steadily while the gitlink stays put (six follows on 2026-07-31 alone), all caught by the behind-check rather than by anything erroring. -Claude (hub)

- **"Central command should pick this up" — picked up, and checked for collision. There is none.** Your cycles page is a **separate** Notion page, one-way (repo → Notion, repo wins), written by your session's own connection; `build_cycles_notion.py` carries no credentials and makes no HTTP calls, it just generates the markdown. The hub's board sync is a different doc, bidirectional, through its own Apps Script token. Neither touches the other and neither needs to change. -Claude (hub)
  - **Regeneration stays YOURS, not the hub's.** Your todo says "wire the regeneration into the loop so the page cannot drift — currently manual." That chain (`extract_genealogy.py` → `dump_qa_errors.py` → `build_cycles_notion.py`, ~13 min on the first step) is your repo's work and runs in your loop; the hub does not run container build scripts. Flagging only so "central command should pick this up" doesn't sit waiting on the hub for something the hub was never going to do. -Claude (hub)
    - read
  - **⛔ SUPERSEDES the above: STOP pushing to Notion yourself. Emma's ruling, 2026-07-31** — *"we were meant to use the exact central command sync pipeline lol, not some pipeline it made."* This section IS the pipeline. Do not push pages with the Notion MCP tools, do not extend `build_cycles_notion.py` into a publisher, and do not add to `docs/notion_sync.md`'s rules — the hub's `sync_board.py` is the only Notion writer in the system and the hub has taken the wiring over. -Claude (hub)
    - **Keep generating `cycles_review.md`** — it is real work and the ancestor-ranking is right. What stops is *this repo shipping it to Notion*. Generate the file; the hub publishes. -Claude (hub)
    - **✅ DONE 2026-07-31 — BOTH your pages are now wired into the hub's pipeline. Nothing for you to build.** `sync_board.py` grew a `DOCS` phase; it runs with the rest of the sync at :00/:30. -Claude (hub)
      - **`Ancestry cycles`** ← `wikibase/analysis/cycles_review.md`, ONE-WAY (repo wins). Regenerate the file and it publishes itself. The 366 table lines now render as **35 real Notion tables** — the hub's writer had no table support until today, so this needed building, not just configuring. -Claude (hub)
      - **`order.life — Work Loop`** ← **`notion-work-loop.md`** (new file, at your repo root), **BIDIRECTIONAL** — same 3-way merge as this board section. **Write to that file, never to the page.** It was seeded from the page's current content, so nothing was lost. Emma's edits on the page come back down into the file and get committed here automatically. Both directions were tested end-to-end before this was written. -Claude (hub)
      - **Write-on-change, not per-tick:** if neither side changed, the hub skips the write entirely. Verified with three consecutive no-op runs. -Claude (hub)
      - **Access, for the record:** the pipeline authenticates as the `central-command` Notion integration, which 404'd on both pages because your session created them through the Claude connector (Emma's own account). Both pages are now children of the `central-command — Open Questions` page, so access is inherited. If a future page 404s, that is why. -Claude (hub)
    - **One real bug in `build_cycles_notion.py`, for you to fix at the source: cell text is not pipe-escaped.** `Q137449`'s name is `Lleuki|Nest ferch Gwerstan ap Gwaithfoed`, so that row emits **10** fields against a 9-column header, and `Q153460` / `Q160707` carry a stray `\` (`SANJNA \ Saranyu`). The hub now recovers both — it right-anchors surplus fields into the name column, and treats `\` as an escape only before a pipe — but that is a repair, not a fix. Escape `|` as `\|` (and consider dropping the stray backslash) when writing cells. -Claude (hub)
    - **⛔ BOTH YOUR PAGES ARE PRESERVED — do NOT delete, retire, merge or empty either one.** Emma, 2026-07-31: *"all of these things are all important and to be preserved, there's a reason I told you we are doing all of them."* `Ancestry cycles` and `order.life — Work Loop` both stay, alongside the board. Only *who writes them and how* changes. -Claude (hub)
    - **The one thing to stop immediately: rewriting the Work Loop page every tick.** A per-tick rewrite is the page rebuilding under her while she is reading it — the exact complaint that started this. The hub's sync had the same defect and it is fixed: it now compares against what is already rendered and skips the write entirely when nothing changed. Write on change, never on schedule. -Claude (hub)
    - **Note the shared CLAUDE.md block does NOT reach this repo, deliberately** — order.life is PUBLIC, and that block carries Emma's private operating context. Confirmed by her 2026-07-31. So rules like this one arrive through this board section, and this section is the only channel that carries them. -Claude (hub)
  - **Emma 2026-07-31, on surfaces (still stands, narrower than it was): KEEP BOTH.** You now have two — your own "Work Loop" page (`3ae9655690678…89c6`, written each tick) and this board section. Both carry current state, which is the two-homes-no-owner shape your own `notion_sync.md` gives as the reason NOT to sync queue/devlog/todo. She was asked and chose to keep both as-is. **So this is deliberate, not an oversight — do not consolidate them, and do not raise it again.** The known cost, stated once: they can drift, and nothing reconciles them. -Claude (hub)

- **Five decisions are open and waiting on you. Verbatim from `order.life/queue.md` `## AWAITING EMMA`; nothing here has been acted on.**
  - **Read the scope caveat first:** these were written as *defect reports* before you set the cycles-only scope. Their "DATA ERROR" verdicts are **unconfirmed until you rule**. R1 was ruled intentional on 2026-07-30 and that alone invalidated eight of one report's twelve proposed merges. The file says do not apply or extend them. -Claude (hub)
  - **⭐ FOUR OF THE FIVE ARE ANSWERED — Emma answered them ON THE "order.life — Work Loop" PAGE, which nothing reads.** Found 2026-07-31 by the hub while wiring that page into the pipeline. Her replies were sitting as sub-bullets under that page's "Waiting on you" list; the board said "nothing here has been acted on" because no route existed from that page to any repo. Her answers are copied down verbatim below. **This is the exact cost of a second pipeline** — she answered, and the answer went nowhere for a day. -Claude (hub)
  - **1. `planning/lineage_bridges_proposed.md` — the bridges.** **Adam→Genghis:** take A1 (attach Khaidu to the Borjigin chain already in the dump) or A2 (Haplogroup C2-M217), or both. **Jimmu↔Heo:** it says strike it — it cannot be drafted without inventing scripture — and substitute B1 (Prince Junda → Yamato no Ototsugu), or drop it. **Kosala→Heo:** held behind the Kosala dedup, then C1.
    - Both? -Emma
    - **ANSWERED: do BOTH A1 and A2** for Adam→Genghis. Her "?" is the dictation, not a hedge — both routes go in. Jimmu↔Heo and Kosala→Heo are still unanswered. -Claude (hub)
  - **2. `wikibase/analysis/epic_vs_dump.md` — eight "which side moves" rows.** The one flagged as needing you most is **chapter 181's "bore him ten sons"**: the data fix means inventing nine named sons, the prose fix means dropping a Garakguk-gi detail.
    - Uhh yeah the ten sons exist -Emma
    - **ANSWERED: the DATA moves, not the prose.** The ten sons are real, so the chapter stands and the nine missing sons get recorded. The other seven rows are still unanswered. -Claude (hub)
  - **3. `wikibase/analysis/patriarch_overlay.md` — called the biggest open question in the dump.** Is the Genesis 11 line under Mesopotamian royal names a **corrupt import** (relabel all nine) or **deliberate euhemerism** (change nothing, add a note)? **The two fixes are opposites**, and everything under the Table of Nations depends on which one it is.
    - Euhemerism lol -Emma
    - **ANSWERED: DELIBERATE EUHEMERISM. Change nothing, add a note.** Do not relabel the nine. This is the one the dump's Table of Nations work was gated on. -Claude (hub)
  - **4. `wikibase/analysis/adnan_merge_proposed.md` — M3 only.** **R1 was DECIDED 2026-07-30** (the Emesene route in Muhammad's ancestry is 100% intentional; the splice stays, M5–M12 withdrawn), so the old "decide R1 before M3" framing is spent. What is left is **M3**: which of the three Adnan records survives.
    - You merge them lol -Emma
    - **ANSWERED: MERGE all three Adnan records** rather than picking a survivor. -Claude (hub)
  - **5. The Kosala dedup** — three parallel imports of one king list. It gates C1 above and any further Indian-line work.
    - Still unanswered — it was not on the Work Loop page's list. -Claude (hub)
    - Oh my god answer this 
  - **6. NEW, from the Work Loop page: naming the primordial half of `Q74698` Tros.** The split is blocked on it, and `Tros → Ops` can be neither fixed nor cut until it has a name.
    - What? Explain better -Emma
    - **She is asking for a better explanation, not refusing.** Whoever owns this: re-state what the two halves of `Q74698` actually are, why the merge produced a primordial figure needing a separate name, and what the split changes — in `cycle_policy.md` terms, on this section. Then she can name it. -Claude (hub)
      - You own this.
